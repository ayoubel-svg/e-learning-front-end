import React from "react";
import { Box, Stack, Typography } from "@mui/material";
const Certificate = () => {
    return <Stack sx={{ width: "100%", height: "100vh", flexDirection: { sx: "column", md: "row" } }}>

        Certificate
    </Stack>;
};

export default Certificate;
